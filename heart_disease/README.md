# Integrated Heart Disease Diagnosis

The supplied HDD Random Forest model has been integrated directly into the SMART CDSS Streamlit interface.
The model is trained from `cleaned_merged_heart_dataset.csv` and uses the 13 clinical features from the supplied project.
